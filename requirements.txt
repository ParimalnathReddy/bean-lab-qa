# PDF Processing Dependencies for LLM QA System
# Install with: pip install -r requirements.txt

# Core PDF processing
PyPDF2>=3.0.0
pypdf>=3.0.0

# Text tokenization
tiktoken>=0.5.0

# Progress tracking
tqdm>=4.65.0

# OCR capabilities (optional - for scanned PDFs)
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=10.0.0

# LLM and embeddings
sentence-transformers>=2.2.0
transformers>=4.30.0
torch>=2.0.0

# Vector database
chromadb>=0.4.0

# BM25 keyword index for hybrid retrieval (Change 2)
rank-bm25>=0.2.2

# RAGAS evaluation pipeline (Change 4) — eval_ragas.py only, not used by the
# core pipeline or either deployed app. PINNED TIGHTLY: this dependency chain
# is fragile — ragas 0.2.x requires langchain-core<0.4, which conflicts with
# langchain-google-genai>=3.0 (needs langchain-core>=1.0). Verified as an
# actually-installable, importable combination; do not bump these
# individually without re-checking they still resolve together.
ragas==0.2.15
langchain==0.3.25
langchain-community==0.3.7
langchain-core>=0.3.17,<0.4.0
langchain-google-genai==2.0.11

# Additional utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Data handling
numpy>=1.24.0
pandas>=2.0.0

# HTTP requests (for document fetching if needed)
requests>=2.31.0

# YAML config files
pyyaml>=6.0


